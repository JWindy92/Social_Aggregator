include mixins/_navbar

html
    head
        title=title
    body
        +navigation()
    
        if current_user
            h1 Hello #{current_user.username}!!!
        else
            h1 Welcome stranger!!