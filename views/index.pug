html
    head
        title=title
    body
        navbar
            a(href="/") Home
            a(href="/login") Login
            a(href="/register") Sign Up
    
        if current_user
            h1 Hello #{current_user.username}!!!
        else
            h1 Welcome stranger!!